import SectionPlaceholder from "../../../components/admin/SectionPlaceholder";

export default function AdminSite() {
  return (
    <SectionPlaceholder
      title="Site"
      description="Manage runtime-configurable portfolio properties."
    />
  );
}