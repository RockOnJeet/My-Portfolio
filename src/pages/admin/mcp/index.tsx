import SectionPlaceholder from "../../../components/admin/SectionPlaceholder";

export default function AdminMcp() {
  return (
    <SectionPlaceholder
      title="MCP"
      description="Manage the MCP server, endpoint, access policy, and exposed tools."
    />
  );
}