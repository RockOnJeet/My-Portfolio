import SectionPlaceholder from "../../../components/admin/SectionPlaceholder";

export default function AdminIntegrations() {
  return (
    <SectionPlaceholder
      title="Integrations"
      description="Manage external services, runtime availability, connection state, and capabilities."
    />
  );
}